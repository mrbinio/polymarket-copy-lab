# Zdalnie = internet, nie to samo Wi‑Fi

LAN (`192.168.0.48`) **nie** jest zdalnym dostępem. Tunel `*.lhr.life` też nie — pada ze snem Maca i zmienia adres.

Prawdziwe zdalne, które da się dać Mitchowi z LTE / innego kraju:

1. **GitHub (prywatne repo)** — wszystkie `docs/*.md`, handoffy, pick PolyCop. Mitch = collaborator, czyta z przeglądarki bez Twojego Wi‑Fi.
2. **Cloudflare Pages** (`ops/` jako root) — strona z wykresami pod stałym `*.pages.dev`, Mac może być wyłączony.

Repo docelowe: `https://github.com/mrbinio/polymarket-copy-lab` (private).

Agent wypycha pliki **sam** po Twoim jednorazowym `gh` login (kod urządzenia). Nie ma klikania Source Control w Cursorze.

---

# PolyCop

Patrz `docs/POLYCOP-PICK.md`. Włączasz tylko Ty w `@PolyCop_BOT`.
